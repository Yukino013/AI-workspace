import type { Request, Response } from 'express'; import { listHistory } from '../services/history.service.js'; import { ok } from '../utils/response.js';
export async function list(req: Request, res: Response) { const page = Math.max(1, Number(req.query.page) || 1); const pageSize = Math.min(100, Math.max(1, Number(req.query.pageSize) || 20)); const type = typeof req.query.type === 'string' ? req.query.type : undefined; const keyword = typeof req.query.keyword === 'string' ? req.query.keyword.trim() : undefined; res.json(ok(await listHistory(req.user!.userId, { page, pageSize, type, keyword }))); }
